# Figure 1 Caption

Corrected full-scale single-overlay recovery for the reduced-scope `J/Q` path. Bars compare prompt-only, direct, and `SOAR` systems on the active operational metric and on answer F1. In both overlays, prompt-only under the same output contract is near zero on the operational metric, while `SOAR` overlaps with the direct single-overlay adapter. Source data: `figures/figure1_single_overlay_recovery.csv`.
