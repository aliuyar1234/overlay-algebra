experiment_name: transfer_slice_eval
milestone: M7
command: overlay_algebra.eval.run
seed: 17
notes: Optional transfer-slice evaluation template. Update adapter_dir and dataset path once the transfer slice exists.
paths:
  eval_dataset: data/processed/transfer_slice/test.jsonl
  adapter_dir: runs/oa_m5_full_triple_17/compositions/JCQ/soar/adapter
payload:
  base_model_name_or_path: Qwen/Qwen2.5-7B-Instruct
  local_files_only: true
  condition: JCQ
  max_length: 768
  max_new_tokens: 96
  max_eval_examples: 500
  eval_batch_size: 4
  device: cuda
  torch_dtype: bf16
