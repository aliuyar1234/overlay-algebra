experiment_name: pilot_residualize_j_smoke
milestone: M3
command: overlay_algebra.analysis.residualize
seed: 17
notes: >
  Smoke-scale M3 residualization run for the JSON overlay using the saved M2 scaffold and direct JSON adapters.
  This is plumbing validation on the bundled fixture shard, not the full pilot split.
paths:
  scaffold_adapter: runs/oa_m2_smoke_scaffold_17/adapter
  overlay_adapter: runs/oa_m2_smoke_json_17/adapter
  eval_dataset: data/processed/squad_jcq_v1_fixture/val.jsonl
payload:
  base_model_name_or_path: Qwen/Qwen2.5-7B-Instruct
  overlay_name: J
  local_files_only: true
  beta_grid:
    - 0.5
    - 0.75
    - 1.0
    - 1.25
  residual_rank: 16
  composed_rank: 32
  synthesized_lora_dropout: 0.0
  semantic_guardrail_points: 3.0
  max_length: 192
  max_new_tokens: 48
  max_eval_examples: 1
  device: cuda
  torch_dtype: bf16
