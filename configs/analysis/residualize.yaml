experiment_name: residualize_scaffold
milestone: M0
command: overlay_algebra.analysis.residualize
seed: 17
notes: Placeholder residualization config for M0. M3 will add dense-delta extraction and TSVD settings.
paths:
  scaffold_adapter: runs/scaffold
  overlay_adapter: runs/full_overlay
  output_root: runs
payload:
  beta_grid:
    - 0.5
    - 0.75
    - 1.0
    - 1.25
  residual_rank: 16
